import { StyleSheet, Text } from "react-native";

export default function VideoPreview() {
    return (
        <Text style={styles.text}>hello preview</Text>
    )
}

const styles = StyleSheet.create({
    text: {
        color: '#ffffff'
    }
})